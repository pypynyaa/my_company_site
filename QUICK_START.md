# 🚀 Быстрый старт - Настройка Telegram

## Шаг 1: Добавьте токен в .env

Создайте файл `.env` в корне проекта и добавьте:

```env
# Ваш токен бота (получен через @BotFather)
VITE_TELEGRAM_BOT_TOKEN=8349789731:AAFWizep1vxd8Dxd0T8HFleDY7YtXx_lNko

# ID вашего канала/группы (получите через @userinfobot или API)
VITE_TELEGRAM_CHAT_ID=@your_channel_name
# ИЛИ для группы:
# VITE_TELEGRAM_CHAT_ID=-1001234567890

# Supabase (если еще не настроено)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_key
```

## Шаг 2: Создайте канал/группу в Telegram

1. Создайте **публичный канал** или **группу** в Telegram
2. Добавьте вашего бота как **администратора**
3. Получите Chat ID:
   - Для канала: используйте @username (например: `@my_friends_channel`)
   - Для группы: добавьте @userinfobot и он покажет ID (например: `-1001234567890`)

## Шаг 3: Обновите базу данных

Выполните SQL из `DATABASE_SCHEMA.sql` в Supabase, чтобы добавить поле `telegram_message_id`:

```sql
ALTER TABLE memories 
ADD COLUMN IF NOT EXISTS telegram_message_id INTEGER;
```

## Шаг 4: Перезапустите сервер

```bash
npm run dev
```

## Готово! ✨

Теперь при добавлении воспоминания:
- Текст автоматически отправляется в Telegram как сообщение
- Фото автоматически загружается в Telegram
- Видео автоматически загружается в Telegram
- В базе сохраняется только `message_id` и метаданные

**Все контент хранится в Telegram бесплатно!** 🎉

## Важно

⚠️ **Токен бота** - это секретная информация. Никогда не коммитьте файл `.env` в Git!

✅ Файл `.env` уже добавлен в `.gitignore`

