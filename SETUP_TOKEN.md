# 📝 Инструкция по настройке токена Telegram бота

## Где вставлять токен?

Токен нужно добавить в файл `.env` в корне проекта.

## Шаг 1: Создайте файл .env

В корне проекта (там же, где `package.json`) создайте файл `.env`:

```bash
# Если файла нет, создайте его
touch .env
```

## Шаг 2: Добавьте токен в .env

Откройте файл `.env` и добавьте следующее:

```env
# Supabase Configuration (обязательно)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Telegram Bot Configuration (для хранения контента в Telegram)
VITE_TELEGRAM_BOT_TOKEN=ваш_токен_бота_здесь
VITE_TELEGRAM_CHAT_ID=ваш_chat_id_здесь
```

## Шаг 3: Получите токен бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Friends Story Bot")
   - Введите username бота (должен заканчиваться на `bot`)
4. BotFather предоставит вам **токен** (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. Скопируйте токен и вставьте в `.env` файл:

```env
VITE_TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

## Шаг 4: Получите Chat ID группы/канала

### Вариант 1: Через бота @userinfobot

1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в вашу группу
2. Отправьте любое сообщение в группу
3. Бот покажет ID группы (начинается с `-` для групп)

### Вариант 2: Через API Telegram

1. Отправьте сообщение в группу с вашим ботом
2. Выполните запрос:
```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates"
```
3. Найдите в ответе `"chat":{"id":-123456789}` - это и есть Chat ID

### Вариант 3: Через веб-интерфейс

1. Создайте группу в Telegram
2. Добавьте вашего бота в группу как администратора
3. Используйте любой из методов выше для получения ID

## Шаг 5: Добавьте Chat ID в .env

```env
VITE_TELEGRAM_CHAT_ID=-123456789
```

⚠️ **Важно**: 
- Для групп ID начинается с минуса (`-123456789`)
- Для каналов ID начинается с минуса и слова `-100` (`-100123456789`)
- Для личных чатов ID - просто число (`123456789`)

## Шаг 6: Перезапустите сервер разработки

После добавления переменных в `.env`:

```bash
# Остановите сервер (Ctrl+C)
# Затем запустите снова
npm run dev
```

## Проверка настройки

После настройки вы должны увидеть в консоли браузера (F12):
- ✅ Если токен настроен: никаких предупреждений
- ⚠️ Если токен не настроен: предупреждение "Telegram Bot Token не настроен"

## Пример полного .env файла

```env
# Supabase
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Telegram
VITE_TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
VITE_TELEGRAM_CHAT_ID=-123456789
```

## 🔒 Безопасность

⚠️ **ВАЖНО**: 
- Никогда не коммитьте файл `.env` в Git!
- Файл `.env` уже добавлен в `.gitignore`
- Токен бота дает полный доступ к боту - храните его в секрете

## Что делать, если токен не работает?

1. Проверьте, что токен скопирован полностью (без пробелов)
2. Убедитесь, что бот добавлен в группу как администратор
3. Проверьте, что Chat ID правильный (для групп должен начинаться с `-`)
4. Перезапустите сервер разработки после изменения `.env`

## Дополнительная помощь

Если у вас возникли проблемы:
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что переменные окружения загружаются (должны начинаться с `VITE_`)
3. Проверьте, что файл `.env` находится в корне проекта

